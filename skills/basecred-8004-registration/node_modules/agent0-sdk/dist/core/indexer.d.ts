/**
 * Agent indexer for discovery and search functionality
 * Simplified version focused on subgraph queries (no local ML indexing)
 */
import type { AgentSummary, SearchFilters, SearchOptions } from '../models/interfaces.js';
import type { AgentId, ChainId } from '../models/types.js';
import { SubgraphClient } from './subgraph-client.js';
/**
 * Simplified indexer that primarily uses subgraph for queries
 * No local indexing or ML capabilities - all queries go through subgraph
 */
export declare class AgentIndexer {
    private subgraphClient?;
    private subgraphUrlOverrides?;
    private readonly defaultChainId?;
    private static readonly BATCH_SIZE;
    constructor(subgraphClient?: SubgraphClient | undefined, subgraphUrlOverrides?: Record<ChainId, string> | undefined, defaultChainId?: ChainId | undefined);
    /**
     * Get agent summary from index/subgraph
     */
    getAgent(agentId: AgentId): Promise<AgentSummary>;
    /**
     * Search agents with filters
     */
    searchAgents(params?: SearchFilters, options?: SearchOptions): Promise<AgentSummary[]>;
    private _parseSort;
    private _resolveChains;
    private _fetchAllAgentsV2;
    private _normalizeAgentIds;
    private _toUnixSeconds;
    private _buildWhereV2;
    private _compareAgents;
    private _intersectIds;
    private _utf8ToHex;
    private _prefilterByMetadata;
    private _prefilterByFeedback;
    private _searchUnifiedNoKeyword;
    private _searchUnifiedWithKeyword;
    private _filterAgents;
    /**
     * Get all configured chains (chains with subgraph URLs)
     */
    private _getAllConfiguredChains;
    /**
     * Get subgraph client for a specific chain
     */
    private _getSubgraphClientForChain;
}
//# sourceMappingURL=indexer.d.ts.map