import { type Hex } from 'viem';
/**
 * Normalize ECDSA signatures to use v = 27/28 (some contracts/libraries expect this).
 *
 * Some signers return v in {0,1}. This function converts those to {27,28}.
 */
export declare function normalizeEcdsaSignature(signature: Hex): Hex;
export declare function recoverMessageSigner(args: {
    message: string | Uint8Array;
    signature: Hex;
}): Promise<`0x${string}`>;
export declare function recoverTypedDataSigner(args: {
    domain: Record<string, unknown>;
    types: Record<string, Array<{
        name: string;
        type: string;
    }>>;
    primaryType: string;
    message: Record<string, unknown>;
    signature: Hex;
}): Promise<`0x${string}`>;
//# sourceMappingURL=signatures.d.ts.map