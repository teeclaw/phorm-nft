import type { ChainClient, ChainReceipt, Hex } from './chain-client.js';
export type TransactionWaitOptions = {
    timeoutMs?: number;
    /**
     * Number of confirmations to wait for (1 = mined).
     */
    confirmations?: number;
    /**
     * If true (default), throw if the receipt indicates the tx reverted.
     */
    throwOnRevert?: boolean;
};
export type TransactionMined<T> = {
    receipt: ChainReceipt;
    result: T;
};
export declare class TransactionHandle<T> {
    private readonly chainClient;
    private readonly computeResult;
    readonly hash: Hex;
    private readonly memo;
    constructor(hash: Hex, chainClient: ChainClient, computeResult: (receipt: ChainReceipt) => Promise<T> | T);
    waitMined(opts?: TransactionWaitOptions): Promise<TransactionMined<T>>;
    /**
     * Alias of waitMined (naming convenience).
     */
    waitConfirmed(opts?: TransactionWaitOptions): Promise<TransactionMined<T>>;
}
//# sourceMappingURL=transaction-handle.d.ts.map