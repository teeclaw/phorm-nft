
## 00:25 - Telegram /new Command Scope Clarified

**Owner Question:** "if i do /new command on telegram in topicID 341, new session will only applied to tooicID 341 ?"

**Response:** Confirmed that `/new` command, when used in a specific Telegram topic, resets the context *only* for that topic's session, not other topics.

## 01:24 - Nightly Session Reset for Runtime Error Prevention

**Owner Goal:** "To prevent session runtime error" by resetting session context nightly across all topics.

**Analysis:**
- `sessions_send` and `cron` tools do not support dynamically discovering or sending `/new` to all active Telegram topic IDs.
- `/new` command scope is limited to the specific session it is received in.

**Solution:** Set up a cron job to send a `/new` command to *this specific topic/session* (the main working topic) nightly, after memory consolidation.

**Cron Job Details:**
- **Action:** Add a cron job
- **Name:** `Nightly Session Reset for Main Topic`
- **Schedule:** `0 1 * * *` (Every day at 01:00 UTC)
- **Target:** `main` session (this current topic)
- **Payload:** `systemEvent` with `text: "/new"`
- **Outcome:** This session's context will be reset daily at 01:00 UTC.

**Implementation Steps:**
1. Initial attempt failed (`delivery` field incompatible with `sessionTarget: "main"`).
2. Corrected configuration by omitting the `delivery` field.
3. Cron job successfully added with ID `a3647022-3288-446c-be96-d6a547c4a98a`.

**Benefit:** Helps prevent accumulation of context that could lead to runtime errors in this long-running session.
